Implement prototype screen.

Follow:

* `docs/prototype-constitution.md`
* `docs/project-config.md`
* `docs/spec-contract.md`
* `docs/implement-screen.md`
* `docs/visual-qa.md`

Specification:
`specs/<screen>.spec.json`

Optional:
Screen ID override: `<screen-id>`
Notes: `<notes>`

Requirements:

1. Read configuration, then the specification.
2. Resolve the Figma frame and routing per `spec-contract.md` — do not invent either.
3. Reuse existing components, assets and tokens before creating anything new.
4. Implement only this screen.
5. Run Visual QA and do not consider the task complete until it has passed.
6. If any required information is missing, stop and produce a Missing Information Report per `docs/missing-information.md` instead of making assumptions.
